"""These tests verify the desired usage of components of PandAna."""

import pandana3.core.var

# import panadana3.core.cut

pt = pandana3.core.var.SimpleVar("electrons", ["pt"])
# hard = pandana3.core.cut.SimpleCut(lambda pt: pt > 25.0)
